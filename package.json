{
  "name": "frtb-sbm-redis-pov",
  "version": "0.1.0",
  "private": true,
  "description": "HSBC FRTB-SA SBM PoV on Redis Enterprise — sales vehicle demo",
  "workspaces": [
    "services/*",
    "shared/*",
    "tools/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "schema:generate": "npm run -w @frtb/schema-cli generate --",
    "schema:validate": "npm run -w @frtb/schema-cli validate --"
  },
  "devDependencies": {
    "vitest": "^4.1.7",
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
