## Verdict: 🟢 GREEN — 9/9 calc HTTP 200 with strictly positive charges and `count > 0` on every per-bucket entry, **grand_total_l2 = 9558.91465449378** reproduces exactly across two consecutive runs, Δ-memory 23.99 MB (79.96 % of the 30 MB cap), no runtime helpers required.

Wave 5.17d re-ran the full {GIRR, EQUITY, FX} × {Delta, Vega, Curvature} matrix against the live-standalone Redis Cloud DB to capture the **current canonical math** after Wave 5.17a reshaped `risk_value` to the object-keyed-by-tenor form. Run-1 produced the 9 raw `calc-*.json` files + `aggregate.json` saved alongside this SUMMARY; run-2 (identical procedure) is preserved under `logs/run-2/` and reproduces `grand_total_l2 = 9558.91465449378` bit-for-bit. **smoke-run-17 supersedes smoke-run-16** — the 9,665.70 grand L2 recorded there was from a pre-Wave-5.17a state that no tagged commit on `main` reproduces (see Wave 5.17a3 investigation in task note `9ffd9dd4-9d07-4fd8-8543-834769840d26`).

# Wave 5.17d — Smoke Re-run #17 (Delta + Vega + Curvature, 9-variant, current canonical math) — SUMMARY

Run date: 2026-06-01T17:10Z (2 000 rows × 3 sensitivity types = 6 000 rows on the standalone Redis Cloud DB)
Generated by: Wave 5.17d implementor (no `services/*` src/ changes; docs-scoped helpers under `docs/recordings/smoke-run-17/scripts/` mirror smoke-run-16/scripts/; api image rebuilt via `docker compose up -d --build api` so the live FRTB library matches current main).

## Named gap (operational, not a verdict downgrade)

Smoke-run-16's `frtb-sbm-redis-pov-api:latest` image was a pre-Wave-5.17a build whose Lua kernels (`services/calc/lib/{girr,equity,fx}_delta.lua`, `…_vega.lua`) read `risk_value` as a plain array. Wave 5.17a (commit on current `main`) reshaped production rows to the object-keyed-by-tenor form `{"3M": v0, "6M": v1, …}` and rewrote the kernels to iterate by tenor label (`rv[tenors[k]]`). Until the api image is rebuilt to embed the current `services/calc/lib/*.lua` (Wave 5.17a kernels) AND the bootstrap loader passes the tenor labels through `buildFrtbSnippets`, a first-pass calc against the stale library reports `K_b = S_b = 0` on every bucket of every Delta/Vega variant (Curvature still works — its kernels were always object-form). Reproducer (first-pass against stale image, captured this wave):

| risk_class | leg       | HTTP | charge                  |
|------------|-----------|:----:|------------------------:|
| GIRR       | Delta     | 200  | 0                       |
| GIRR       | Vega      | 200  | 0                       |
| GIRR       | Curvature | 200  | 9 495.234078557116      |
| EQUITY     | Delta     | 200  | 0                       |
| …          | …         | …    | …                       |

Workaround used this wave (no `services/*` change): `docker compose up -d --build api` rebuilds the image from the current source tree, the bootstrap fires `FUNCTION LOAD REPLACE` with the Wave 5.17a snippets on api startup, and the next 9-variant matrix returns the canonical numbers. Operators on smoke-run-16's stale image will see the same Delta/Vega = 0 first-pass symptom; the fix is the rebuild, no schema or `services/*` change is required.

## Pre-flight (blocking gates)

| # | Gate | Verdict | Evidence |
|--:|------|:-------:|----------|
| 1 | `.env.local` has `REDIS_CLUSTER=FALSE` AND `REDIS_TLS=FALSE` | ✅ | Both values present (host-side `grep`; values not echoed). |
| 2 | Standalone Redis Cloud DB reachable, `DBSIZE=0` after FLUSHALL | ✅ | `scripts/smoke-reset-cluster.sh --yes` logs `XLEN sensitivities:in before/after: 6000 / 0`, `FT._LIST sanity: OK`. |
| 3 | `services/generator/src/row-generator.ts` supports `sensitivityTypes: ["Delta"\|"Vega"\|"Curvature"]` | ✅ | Wave 5.16d + Wave 5.17a in current `main`; verified by running the three `ingest-typed.ts` legs and checking per-`sensitivity_type` FT.SEARCH count. |
| 4 | `services/api/src/bootstrap.ts:buildFrtbSnippets` registers all 9 snippets at api startup (Wave 5.16f1) | ✅ | `FUNCTION LIST LIBRARYNAME frtb` afterwards lists all 9 functions including `{girr,equity,fx}_curvature`. |
| 5 | Live FRTB library is the Wave 5.17a object-keyed-by-tenor kernel | ✅ | `FUNCTION LIST … WITHCODE` contains `rv[tenors[k]]` and the Wave 5.17a header. |

## Per-step verdict

| Step | Action | Verdict | Notes |
|---:|--------|:-------:|-------|
| 1 | `FLUSHALL` via `scripts/smoke-reset-cluster.sh --yes` | ✅ | `XLEN sensitivities:in before/after: 6000 / 0`, `FT._LIST: 0 indexes`, `DBSIZE = 0`. |
| 2 | `docker compose up -d --build api` (force image refresh) | ✅ | Recreates the api container from the current source tree so `services/calc/lib/*.lua` Wave 5.17a kernels enter the bootstrap loader. |
| 3 | Capture B' (post bootstrap, pre-ingest) | ✅ | `logs/used-memory-baseline.json`: `used_memory_bytes = 8 348 704` / `used_memory_human = 7.96M`. `num_docs = 0`, `xlen_stream = 0`. |
| 4 | Ingest 2 000 Delta + 2 000 Vega + 2 000 Curvature via `ingest-typed.ts` (3 invocations, seed = 0) | ✅ | `logs/ingest-Delta.json` (1 294 rps), `logs/ingest-Vega.json` (1 333 rps), `logs/ingest-Curvature.json` (1 329 rps). Per-leg class mix = GIRR 667 / EQUITY 667 / FX 666. |
| 5 | Wait for `idx:sens.num_docs == 6 000` | ✅ | Already at 6 000 at first poll (t=2 s). `XLEN sensitivities:in = 6 000`, `hash_indexing_failures = 0`, `total_indexing_time = 125.149747 ms`. `logs/ft-info-post-ingest.json`. |
| 6 | Capture P + HARD GUARD vs 30 MB Δ cap | ✅ | `logs/used-memory-post-ingest.json`: `used_memory_bytes = 33 506 736` / `used_memory_human = 31.95M`. **P − B' = 25 158 032 B = 23.99 MB = 79.96 % of the 30 MB Δ cap** → guard PASS (5.99 MB headroom). |
| 7 | Per-`sensitivity_type` FT.SEARCH count check (2 000 each) | ✅ | `FT.SEARCH idx:sens @sensitivity_type:{Delta\|Vega\|Curvature} LIMIT 0 0` → `Delta: 2000`, `Vega: 2000`, `Curvature: 2000`. Per-(risk_class × leg): GIRR 667, EQUITY 667, FX 666 for each leg. |
| 8 | 9-variant calc matrix (single pass) + save 9 raw JSONs | ✅ | 9/9 HTTP 200; all charges strictly positive; every per-bucket entry has `count > 0` and `K_b > 0`. Files: `calc/calc-{GIRR,EQUITY,FX}-{Delta,Vega,Curvature}.json` + `aggregate.json`. |
| 9 | Capture final used_memory post-calc | ✅ | `logs/used-memory-final.json`: `used_memory_bytes = 34 390 752` / `used_memory_human = 32.79M` (Δ vs B' = 24.83 MB; calc fan-out cost = +884 016 B). |
| 10 | Reproducibility — repeat steps 1 + 4 + 5 + 8 against the same image (run-2 under `logs/run-2/`) | ✅ | `grand_total_l2 = 9558.91465449378` bit-for-bit identical; per-class L2 `{GIRR: 9495.37681885356, EQUITY: 367.67854779089924, FX: 1037.0539645403073}` identical. |

## `used_memory` timeline (single shard, single standalone DB)

| Checkpoint | `used_memory_human` | `used_memory_bytes` | Δ-over-B' | % of 30 MB Δ cap |
|------------|---------------------|--------------------:|----------:|-----------------:|
| Pre-reset (carry-over from prior run) | 30.42 M | 31 906 800 | — | — |
| Baseline B' (post `--build api` + bootstrap, pre-ingest) | 7.96 M | 8 348 704 | 0 B | 0 % |
| Post-ingest P (6 000 rows: 2 000 each Delta/Vega/Curvature) | **31.95 M** | **33 506 736** | **25 158 032 B = 23.99 MB** | **79.96 %** ✅ |
| Final (post 9-variant calc matrix, run-1) | 32.79 M | 34 390 752 | 26 042 048 B = 24.83 MB | 82.78 % |

**Per-row cost (measured this run)**: 25 158 032 B / 6 000 rows = 4 193 B/row = **4.10 KB/row**. Marginally heavier than smoke-run-16's 3.86 KB/row because the Wave 5.17a object-keyed `risk_value` shape carries the tenor label strings inline alongside the per-tenor scalar — a deliberate trade favouring kernel readability over wire overhead (no FCALL change at the Lua line-count level). Δ-over-B' headroom = 30 MB − 23.99 MB = **6.01 MB** under the 30 MB cap.

## Δ vs 30 MB Δ-over-baseline cap

**Peak Δ (P − B') = 25 158 032 B = 23.99 MB = 79.96 % of the 30 MB cap → 🟢 GREEN (6 011 968 B = 5.73 MB of headroom).** No HARD GUARD trip at step 6. Slightly tighter than smoke-run-16 (75.32 %) because Wave 5.17a's object-form `risk_value` keys each tenor label as a JSON property (overhead = label bytes × tenor count × row count) rather than relying on declaration order. Headroom remains positive and stable across both back-to-back runs (run-1 final = 24.83 MB, run-2 final = 24.94 MB).

## Calc matrix (9-variant evidence)

| risk_class | leg       | HTTP | wallclock | charge                  | per_bucket count | non-zero buckets | total_ms | fanout_ms |
|------------|-----------|:----:|----------:|------------------------:|-----------------:|-----------------:|---------:|----------:|
| GIRR       | Delta     | 200  |   301 ms  | **0.6846307166088303**  | 11 | 11 / 11 | 300.829 | 177.317 |
| GIRR       | Vega      | 200  |   286 ms  | **52.06012591155659**   | 11 | 11 / 11 | 286.053 | 167.355 |
| GIRR       | Curvature | 200  |   301 ms  | **9 495.234078557116**  | 11 | 11 / 11 | 300.762 | 180.780 |
| EQUITY     | Delta     | 200  |   271 ms  | **6.924412067037891**   | 13 | 13 / 13 | 271.287 | 154.191 |
| EQUITY     | Vega      | 200  |   278 ms  | **15.301745210544523**  | 13 | 13 / 13 | 277.693 | 157.389 |
| EQUITY     | Curvature | 200  |   283 ms  | **367.294736712442**    | 13 | 13 / 13 | 283.289 | 164.171 |
| FX         | Delta     | 200  |   264 ms  | **1.1552805689882315**  | 11 | 11 / 11 | 263.989 | 148.836 |
| FX         | Vega      | 200  |   283 ms  | **15.403740919843086**  | 11 | 11 / 11 | 283.308 | 161.469 |
| FX         | Curvature | 200  |   272 ms  | **1 036.9389159739594** | 11 | 11 / 11 | 272.115 | 153.378 |

**L2 risk-class totals** (`√(Delta² + Vega² + Curvature²)`):
- **GIRR  = 9 495.37681885356**
- **EQUITY = 367.67854779089924**
- **FX     = 1 037.0539645403073**

**Grand-total L2 across risk classes = `9 558.91465449378`** (canonical for current `main`; reproduces bit-for-bit on run-2 — see `logs/run-2/`).

Per-variant wallclock 264–301 ms; fanout 149–181 ms; total_ms 263.989–300.829 ms. Curvature charges remain ~3 orders of magnitude larger than Delta because the §21.5(2) CVR shift uses the price-impact PnL — bp-scale shocks revalued at portfolio bp/bp — so the absolute scale is preserved relative to smoke-run-16 (the recipe is identical; only the seed-driven per-row sample changes).

## Reproducibility (run-2 under `logs/run-2/`)

Identical procedure (steps 1 + 4 + 5 + 8) repeated against the same rebuilt api image:

| risk_class | run-1 charge            | run-2 charge            | match  |
|------------|------------------------:|------------------------:|:------:|
| GIRR.Delta     | 0.6846307166088303      | 0.6846307166088303      | ✅ |
| GIRR.Vega      | 52.06012591155659       | 52.06012591155659       | ✅ |
| GIRR.Curvature | 9 495.234078557116      | 9 495.234078557116      | ✅ |
| EQUITY.Delta   | 6.924412067037891       | 6.924412067037891       | ✅ |
| EQUITY.Vega    | 15.301745210544523      | 15.301745210544523      | ✅ |
| EQUITY.Curvature | 367.294736712442      | 367.294736712442        | ✅ |
| FX.Delta       | 1.1552805689882315      | 1.1552805689882315      | ✅ |
| FX.Vega        | 15.403740919843086      | 15.403740919843086      | ✅ |
| FX.Curvature   | 1 036.9389159739594     | 1 036.9389159739594     | ✅ |
| **grand_total_l2** | **9 558.91465449378** | **9 558.91465449378** | ✅ |

## Repo hygiene

- `git diff --name-only` `services/`: zero entries (no `services/*` src/ change). The five files under `docs/recordings/smoke-run-17/scripts/` (`build-aggregate.mjs`, `connectivity-probe.mjs`, `ft-info.mjs`, `ingest-typed.ts`, `used-memory.mjs`) are docs-scoped helpers; `ft-info.mjs` was extended from smoke-run-16 to auto-detect cluster vs standalone via `REDIS_CLUSTER` (mirrors `used-memory.mjs`'s pattern), the rest are byte-identical to smoke-run-16 modulo the `smoke-run-16 → smoke-run-17` literal-string substitution in `build-aggregate.mjs`.
- Secrets policy: `REDIS_URL` / password / username are loaded from `.env.local` via `set -a; source .env.local; set +a` and never echoed in command argv, script stdout, or any saved JSON.

## Outcome classification

**🟢 GREEN.** `grand_total_l2 = 9558.91465449378` reproduces exactly across two consecutive runs from the current `main` source tree with the live-standalone Redis Cloud DB target. 9/9 calc variants HTTP 200 with strictly positive charges, every per-bucket entry has `count > 0` and `K_b > 0`, P − B' = 23.99 MB < 30 MB cap, the 9 raw `calc-*.json` files + `aggregate.json` are saved verbatim, and the procedure is fully deterministic given `seed = 0`. The operational gap (stale api image) is documented above with a clean one-command fix (`docker compose up -d --build api`); no code change required to reach this verdict on a fresh checkout of current `main`.
