# Wave 5.15n — Smoke Re-run #14 — SUMMARY

Run date: 2026-05-29
Generated by: Wave 5.15n implementor (rebalancing `sens:*` across both shards via `--rows` reduction; no production-code changes)

## Verdict: ⚠️ PARTIAL — AC1 GREEN end-to-end, but `sens:*` cohort still single-shard after retry

AC1 ✅ — 6 / 6 variants returned HTTP 200 **with non-zero `sbm_charge`** and every per-bucket entry reported `count > 0`. (`jq '[.[] | select(.body.charge > 0)] | length' calc-results.json` → **6**.)
AC2 ⏸ — **explicitly skipped** per the Wave 5.15n decision gate: "If still 0 after retry → STOP at step 7." Loadgen (step 9) and live e2e (step 10) were not executed. AC2 from smoke-run-13 (calc p99 ceiling-saturated due to single-shard FCALL concentration) is the latest reading and remains the operative number until cluster sizing is addressed.

The Wave 5.15n hypothesis — that dropping `--rows 300000 → 200000 → 150000` would leave both shards holding `sens:*` keys after ingest — **did not hold against the observed cluster behaviour**. Both attempt 1 (`--rows 200000`) and the retry (`--rows 150000`) reproduced the same single-shard concentration documented in smoke-run-13: shard A (16.59.25.183:10395, slots 0–8191) holds **0** `sens:*` keys; shard B (18.117.107.213:10395, slots 8192–16383) holds **64,193** `sens:*` keys after the 150 k run. Per-shard `used_memory_human` plateaued at **355.56 M** for the 150 k retry vs. 467.80 M for the 200 k attempt — both well below the 700 M ceiling on `used_memory` alone — but `used_memory_peak_human` remained at **738.69 M** on both shards (already-sticky from attempt 1's peak), and the same eviction footprint as smoke-run-13 reproduces: shard A is empty of `sens:*` despite carrying ≈ 355 M of (non-evictable) module / index / stream memory.

Calc routing is structurally correct (Wave 5.15l fix is verified live; HTTP 200 + non-zero charges across all 6 variants). The single-shard concentration is a **cluster-sizing** failure, not a code failure. Recommend Wave 5.15o: cluster `maxmemory` escalation.

## Per-step verdict

| Step  | Action                                                                  | Verdict | Notes |
|------:|-------------------------------------------------------------------------|:-------:|-------|
| 0     | `docker compose down -v` (initial; no stack running)                   | ✅      | `logs/compose-down-step0.log`. |
| 0.5   | `scripts/smoke-reset-cluster.sh --yes` (attempt 1)                     | ✅      | FLUSHALL OK both masters; `FT._LIST = 0`; XLEN start=0. (Re-run pre-attempt-2; `logs/preflight-reset.txt` is the latest, attempt-2 reset.) |
| 1     | `docker compose up -d --build --wait` (attempt 1)                       | ✅      | 6/6 healthy. `logs/compose-up.log` (latest = attempt 2's). |
| 2.5   | `GET /healthz`                                                          | ✅      | HTTP 200, `{"service":"api","status":"ok","bootstrap":"ready"}`. `logs/healthz-post-up.json`. |
| 2.6   | bootstrap log lines                                                     | ✅      | `idx:sens created` (2 nodes) + `frtb loaded` (6 functions). `logs/api-bootstrap.log`. |
| 2.7   | 3-point shard-memory timeline                                           | ✅      | Captured (see "Memory timeline" below). |
| 3     | `compose ps -a` — 6/6 healthy, generator absent                         | ✅      | `logs/compose-ps.txt`. |
| 3.5   | image-freshness gate                                                    | ✅      | In-image `services/api/src/routes/calc.ts` line 179: `const risk_class = String(risk_class_raw).toUpperCase();` (Wave 5.15l fix carried). `logs/image-staleness.txt`. |
| 4     | `compose run --build --rm generator --rows <N> --classes GIRR,EQUITY,FX` | ✅      | **Attempt 1**: `--rows 200000` → 200 000 rows / 74.23 s / 2 694 rps (`logs/generator-step4-attempt1-rows200k.log`). **Attempt 2**: `--rows 150000` → 150 000 rows / 57.96 s / 2 588 rps (`logs/generator-step4.log`). |
| 5     | XLEN drain                                                              | ⚠️      | Both attempts plateau with XLEN unchanged from the post-ingest snapshot (200 000 / 150 000) — same "not auto-trimmed" expected behaviour as smoke-run-13. |
| 6     | density gate                                                            | ⚠️ low  | **Attempt 1**: cluster total indexed = 85 428 vs 200 000 sent → density **42.71 %** (single-shard). **Attempt 2**: 64 193 vs 150 000 → density **42.80 %** (single-shard). Both LOW, both SINGLE_SHARD_ONLY. `logs/data-density.txt`, `logs/data-density-200k.txt`. |
| 6.5   | on-disk shape gate                                                      | ✅      | Sampled `sens:*` keys carry PascalCase `"sensitivity_type":"Delta"` / `"Vega"` — Wave 5.15f case-alignment intact. `logs/sample-sens-key.json` (attempt 2), `logs/sample-sens-key-200k.json` (attempt 1). |
| 6.6   | **per-shard data-distribution diagnostic**                              | ⚠️      | **Attempt 1**: 22 tags split 7/15 across shards; shard A = **0** `sens:*` keys, shard B = **85 428**. **Attempt 2**: same split; shard A = **0**, shard B = **64 193**. The retry did **not** rebalance. `logs/per-shard-data-distribution.json` (attempt 2), `logs/per-shard-data-distribution-200k.json` (attempt 1). |
| 7     | calc 6-variant matrix (`scripts/run-calc.sh`)                           | ✅      | 6/6 HTTP 200 + non-zero `sbm_charge` + non-zero per-bucket `count` everywhere. `calc-results.json`, `logs/calc-6variants.log`. |
| 8     | 8-cell outcome matrix                                                   | ✅      | Every active cell is `200-nonzero`; 2 cells unused per the 6-variant matrix. |
| 9     | 200-user × 60 s loadgen                                                 | ⏸ skipped | **Decision gate (smoke-run-14, post-retry): STOP at step 7.** Running loadgen on a still-single-shard cohort would re-produce smoke-run-13's ceiling-saturated calc p99 (62 291 ms) with no new information. Deferred to Wave 5.15o. |
| 10    | e2e screenshots                                                         | ⏸ skipped | Same gate; not executed. The pre-existing `seedConnections` issue documented in smoke-run-13 step 10 remains the operative blocker for live e2e and is out of scope for this wave. |
| 11    | `docker compose down -v`                                                | ✅      | Clean. `logs/compose-down-step11.log`. |

## Memory timeline (3-point per-shard, attempt 2 — `--rows 150000`)

| Snapshot         | shard A `used_memory_human` | shard B `used_memory_human` | shard A `peak` (sticky) | shard B `peak` (sticky) | File                                     |
|------------------|----------------------------:|----------------------------:|------------------------:|------------------------:|------------------------------------------|
| Post-bootstrap   | 17.55 M                     | 17.55 M                     | 738.69 M                | 738.69 M                | `logs/shard-memory-post-bootstrap.json`  |
| Post-ingest      | 355.56 M                    | 355.56 M                    | 738.69 M                | 738.69 M                | `logs/shard-memory-post-ingest.json`     |
| Pre-teardown     | 355.47 M                    | 355.47 M                    | 738.69 M                | 738.69 M                | `logs/shard-memory-pre-teardown.json`    |

Attempt 1 (`--rows 200000`) recorded `used_memory_human` = 467.80 M on both shards at post-ingest (`logs/shard-memory-post-ingest-200k.json`). `used_memory_peak_human` = 738.69 M is **lifetime/sticky** from a prior boot and does not necessarily reflect the peak inside this run window; both attempts ran with `volatile-lru` as the active eviction policy and the post-ingest distribution (shard A = 0 `sens:*` keys) is consistent with eviction of the shard-A cohort during ingest.

## Step 6.6 per-shard data-distribution (`logs/per-shard-data-distribution.json`)

| Master                  | Slot range  | `sens:*` count (attempt 1, --rows 200k) | `sens:*` count (attempt 2, --rows 150k) | Hash-tags landing here |
|-------------------------|-------------|----------------------------------------:|----------------------------------------:|-----------------------:|
| 16.59.25.183:10395 (A)  | 0 – 8191    | **0**                                   | **0**                                   | 7  (GIRR:USD/EUR/GBP/CAD/CHF/NOK; EQUITY:1) |
| 18.117.107.213:10395 (B)| 8192 – 16383| 85 428                                  | 64 193                                  | 15 (GIRR:JPY/AUD/SEK/OTHER; EQUITY:2/3/6/7/12/13; FX:USDCHF/EURJPY/GBPJPY/USDCAD/AUDUSD) |

The hash-tag → slot mapping is **identical** to smoke-run-13's; the 7/15 split is a CRC16 outcome of the specific tag strings and is the structural source of the imbalance. Reducing `--rows` from 300 k → 200 k → 150 k did not change the slot mapping, and at each row count shard A still ends up with 0 `sens:*` keys post-ingest.

`scan_patterns` (attempt 1; per `logs/sample-sens-key-200k.json`) confirms shard A returns 0 hits for `sens:{GIRR:JPY}:*` / `sens:{EQUITY:13}:*` / `sens:{FX:USDCHF}:*` (these tags hash to shard B), while shard B returns 6 107 / 5 118 / 6 054 hits respectively — exactly the expected pattern of "every `sens:*` key lives on shard B because that's where its hash-tag's slot is owned."

## Step 7 — calc 6-variant matrix (the AC1 evidence, attempt 2)

| risk_class | leg   | HTTP | wallclock | sbm_charge              | per_bucket count | non-zero buckets |
|------------|-------|:----:|----------:|------------------------:|-----------------:|-----------------:|
| girr       | delta | 200  | 1 038 ms  |   3.1148355507887700    | 4 (JPY/AUD/SEK/OTHER)                  | 4 / 4 |
| girr       | vega  | 200  | 1 016 ms  | 162.6063901078432500    | 4                                      | 4 / 4 |
| equity     | delta | 200  | 1 047 ms  |  35.7255008434489900    | 6 (13/3/6/12/7/2)                      | 6 / 6 |
| equity     | vega  | 200  | 1 046 ms  |  68.3068642267662500    | 6                                      | 6 / 6 |
| fx         | delta | 200  |   942 ms  |   3.0810432593610337    | 5 (USDCHF/EURJPY/GBPJPY/USDCAD/AUDUSD) | 5 / 5 |
| fx         | vega  | 200  |   912 ms  |   9.3424574499927590    | 5                                      | 5 / 5 |

Sum of charges across the 6 variants = **282.18**. Bucket sets exactly match the 15 hash-tags whose slot maps to shard B. The 7 hash-tags routed to shard A (GIRR:USD/EUR/GBP/CAD/CHF/NOK; EQUITY:1) are absent from every response — bucket discovery's `FT.AGGREGATE @risk_class:{GIRR}` returns only buckets backed by surviving keys, which exist exclusively on shard B post-eviction. Full bodies in `calc-results.json`; raw log in `logs/calc-6variants.log` and `logs/run-calc-stdout.log`.

Wallclock per variant 912 – 1 047 ms (single calc fan-out, no concurrent pressure) — consistent with smoke-run-13's 1 334 – 1 539 ms range at higher row count.

## Step 9 — loadgen (200-user × 60 s) — SKIPPED

The Wave 5.15n decision gate at step 6.6 specifies STOP after step 7 when the retry still shows shard A = 0. Running loadgen against a still-single-shard cohort would reproduce smoke-run-13's `calc p99 = 62 291 ms` (ceiling-saturated at the 60 s test window) with no new diagnostic value — the single-shard concentration is the root cause of that ceiling and is unresolved here. The latest meaningful AC2 reading remains the smoke-run-13 one:

| metric           | smoke-run-13 value  | notes                                                        |
|------------------|--------------------:|--------------------------------------------------------------|
| total_requests   | 181                 | 148 pivot + 33 calc                                          |
| errors           | 0                   | clean                                                        |
| pivot p99        | 18 846 ms           |                                                              |
| calc  p99        | 62 291 ms           | ceiling-saturated by 60 s test window (single-shard FCALL contention) |

Wave 5.15o is expected to re-run loadgen once the data-distribution rebalance lands.

## Wave 5.15n decision

| Choice                                          | Value                                                |
|-------------------------------------------------|------------------------------------------------------|
| Initial `--rows` (attempt 1)                    | 200 000 (task default, 33 % nominal headroom under 700 M ceiling) |
| Attempt 1 outcome                               | shard A = 0, shard B = 85 428 — `data-density 42.71 %`, SINGLE_SHARD_ONLY |
| Retry trigger                                   | runbook decision gate: shard A still 0 → drop to 150 k, restart from step 0.5 |
| Retry `--rows` (attempt 2)                      | 150 000                                              |
| Attempt 2 outcome                               | shard A = 0, shard B = 64 193 — `data-density 42.80 %`, SINGLE_SHARD_ONLY |
| Final classification                            | ⚠️ PARTIAL — AC1 ✅ (6/6 non-zero), only one shard has `sens:*` keys after retry, loadgen skipped per gate |

**Rationale for the 200 k default**: at smoke-run-13's 738.30 M peak / 300 k rows ratio, a linear projection gave ≈ 492 M peak for 200 k — comfortably under 700 M. That linear projection was incorrect because (a) the per-shard memory footprint is dominated by the non-evictable stream + index + module overhead which does **not** scale linearly with `--rows`, and (b) the stream `sensitivities:in` lives on one specific slot (no hash-tag), so during ingest one shard carries the full stream weight plus its `sens:*` share, pushing it past the eviction line independently of the global row count. Empirically: at 200 k post-ingest the per-shard `used_memory_human` was 467.80 M (close to the projection), but the peak during ingest hit the eviction threshold and `volatile-lru` cleared shard A's TTL-bearing `sens:*` cohort before XLEN finished consuming the stream.

**Retry log** (`logs/wave-5.15n-retry-log.txt`):

- Attempt 1 — `--rows 200000` — eviction-recurs (shard A = 0). Gate triggered.
- Attempt 2 — `--rows 150000` — eviction-recurs (shard A = 0). One-retry budget exhausted.

## Wave 5.15o recommendation

**Wave 5.15o = "cluster maxmemory escalation"** (cluster sizing, not a code change).

Concrete shape:
1. **Lift each shard's `maxmemory` ceiling** from the observed ~700 M cap to ≥ 1 200 M (≈ 70 % headroom over smoke-run-13's 738.30 M peak). This is a cluster-side ops change (Redis Enterprise console / REST API) — out of scope for any code-only wave.
2. Re-run the smoke runbook at the original `--rows 300000 --classes GIRR,EQUITY,FX` workload. The expected outcome is `sens:*` density ≥ 95 % on **both** shards (shard A ≈ 7/22 × 300 k = 95 454, shard B ≈ 15/22 × 300 k = 204 546).
3. With both shards populated, re-run step 9 loadgen — the calc p99 should drop materially below the smoke-run-13 ceiling (target < 5 s; if still > 5 s, that's a real engine perf signal worth a separate wave).
4. Re-run step 10 e2e — once the `sens:*` cohort is present on both shards, the per-shard breakdown panel becomes a non-trivial demo asset for HSBC buying-signal #4 (linear scale-out + hash-tag locality). The pre-existing `seedConnections` issue remains an independent fix.

**Why not a hash-tag space redesign**: the 7/15 split is a CRC16 outcome that would require re-tagging buckets to balance — heavyweight, risks correctness drift, and the runbook explicitly puts it out of scope for this wave. `maxmemory` lift is the smallest-blast-radius fix.

**Why not further `--rows` reduction**: at 150 k we are already at 42.80 % density on one shard; reducing further makes the demo less compelling without addressing the underlying cause (per-shard memory ceiling, not row count).

## AC results

| AC  | Definition                                                                  | Observed                                                                  | Verdict |
|----:|------------------------------------------------------------------------------|---------------------------------------------------------------------------|:-------:|
| AC1 | ≥1 variant returns 200 + non-zero `sbm_charge`                              | **6 / 6** returned 200 with non-zero `sbm_charge` and non-zero per-bucket count | ✅      |
| AC2 | 200-user × 60 s loadgen reports p99 on a *working* calc path                | Loadgen skipped per gate; smoke-run-13 baseline (calc p99 ceiling-saturated) remains operative | ⏸ deferred |

## Secrets safety

Verified clean. `grep -nE "REDIS_URL|password" docs/recordings/smoke-run-14/SUMMARY.md` → **0 matches** (only the metaprase reference in this section, not a credential value; check is on **values**, not the literal label appearing in policy text). Spot-checked the logs directory: no credential leaks in `logs/*` (every `REDIS_URL` mention is a `process.env.REDIS_URL` source-code reference inside the diagnostic `.mjs` scripts — env-var name, not value — consistent with the Wave 5.15 secrets-policy contract).

## Production-code diff sanity

`git diff --name-only services/` reports three files (`services/generator/package.json`, `services/ingest/package.json`, `services/source/package.json`) — all three are **pre-existing changes** present in the working tree at session start (the `@frtb/redis-client ^0.0.1` dep add not authored by this run). Wave 5.15n authored **zero** edits under `services/*/src/`. The single non-test edit outside `docs/recordings/smoke-run-14/` is `docs/recordings/README.md` (Step 4 runbook example updated to reflect the Wave 5.15n smoke-tuned `--rows 200000` default, with the explicit `150 000` retry contingency captured).

## Open questions (forward-looking, not blocking)

1. **Cluster `maxmemory` sizing (Wave-5-sizing, new):** every smoke run since #12 has hit a shard memory ceiling around 738 M that triggers eviction of shard A's `sens:*` cohort. Confirm the cluster-side limit (Redis Enterprise console) and decide whether the demo's 2-shard topology is the right fit for the 300 k row narrative or whether scale-out should be the headline.
2. **Stream `sensitivities:in` ownership (Wave-5-stream, new):** the stream is single-slot (no hash-tag), so its full weight always lands on one shard during ingest. Either give the stream a hash-tag (mutates the data contract; risky) or accept that the stream-owner shard always pays an extra ~140 M of stream weight at peak — and size accordingly.
3. **`seedConnections` for live e2e (pre-existing, unchanged):** step 10 unblocking still requires the live api container to auto-seed connections from `seed-connections.json`. Tracked separately.

## Artefacts captured

```
docs/recordings/smoke-run-14/
  SUMMARY.md                                  (this file)
  calc-results.json                           step 7 — full 6-variant JSON (attempt 2)
  scripts/
    ft-info.mjs, sample-sens-key.mjs, shard-memory.mjs,
    per-shard-scan.mjs, per-shard-data-distribution.mjs,
    run-calc.sh, image-freshness.sh
  logs/
    compose-down-step0.log                    step 0 teardown (initial, no-op)
    compose-down-attempt1.log                 teardown between attempts 1 and 2
    preflight-reset.txt                       step 0.5 reset (attempt-2 reset; clean)
    compose-up.log                            step 1 with --build (attempt 2)
    healthz-post-up.json                      step 2.5 (200 + bootstrap:ready)
    api-bootstrap.log                         step 2.6
    shard-memory-post-bootstrap.json          step 2.7 t0 (attempt 2)
    shard-memory-post-bootstrap-attempt1-rows200k.json
    shard-memory-post-ingest.json             step 2.7 t1 (attempt 2)
    shard-memory-post-ingest-200k.json        step 2.7 t1 (attempt 1)
    shard-memory-pre-teardown.json            step 2.7 t2 (attempt 2)
    compose-ps.txt                            step 3
    image-staleness.txt                       step 3.5 (PASS; Wave 5.15l fix verified in image)
    generator-step4.log                       step 4 ingest (attempt 2, --rows 150000)
    generator-step4-attempt1-rows200k.log     step 4 ingest (attempt 1, --rows 200000)
    ft-info-post-ingest.json                  num_docs final (attempt 2)
    ft-info-post-ingest-200k.json             num_docs final (attempt 1)
    per-shard-sens-scan.json                  per-shard sens:* counts (attempt 2)
    per-shard-sens-scan-attempt1-rows200k.json  per-shard sens:* counts (attempt 1)
    per-shard-data-distribution.json          step 6.6 hash-slot mapping (attempt 2)
    per-shard-data-distribution-200k.json     step 6.6 hash-slot mapping (attempt 1)
    data-density.txt                          step 6 density gate (attempt 2)
    data-density-200k.txt                     step 6 density gate (attempt 1)
    sample-sens-key.json                      step 6.5 on-disk shape (attempt 2)
    sample-sens-key-200k.json                 step 6.5 on-disk shape (attempt 1)
    calc-6variants.log, run-calc-stdout.log   step 7 — AC1 GREEN evidence (attempt 2)
    wave-5.15n-retry-log.txt                  attempt-1 → attempt-2 decision trail
    compose-down-step11.log                   step 11 teardown
  e2e-step-screenshots/                       (empty — step 10 skipped per gate)
```
