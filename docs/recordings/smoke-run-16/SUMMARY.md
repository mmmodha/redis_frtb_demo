## Verdict: ⚠️ DEGRADED — 9/9 calc HTTP 200, Δ-memory 22.59 MB (75.32 % of the 30 MB cap), but the Curvature legs required a docs-scoped runtime helper to register the three `*_curvature` Redis Functions (named gap in `services/api/src/bootstrap.ts`)

Wave 5.16e2 ran the full {GIRR, EQUITY, FX} × {Delta, Vega, Curvature} matrix against the standalone Redis Cloud DB. All six Delta/Vega variants return HTTP 200 out of the box; the three Curvature variants returned `500 — ERR Function not found` until a one-off runtime helper (`docs/recordings/smoke-run-16/scripts/load-curvature-frtb.ts`) reloaded the `frtb` library via `FUNCTION LOAD REPLACE` with the full nine-snippet bundle (the six Delta+Vega ones already in bootstrap.ts plus the three Curvature ones whose builders exist as public exports but are not yet wired into the production bootstrap call list). After that helper ran, 9/9 variants returned HTTP 200 with strictly positive charges and every per-bucket entry reporting `count > 0`. Δ-memory peaks at **22.59 MB = 75.32 %** of the 30 MB cap and the 9 raw `calc-*.json` files + `aggregate.json` are saved alongside this SUMMARY.

# Wave 5.16e2 — Smoke Re-run #16 (Delta + Vega + Curvature, 9-variant) — SUMMARY

Run date: 2026-06-01T09:00Z (2 000 rows × 3 sensitivity types = 6 000 rows on the standalone Redis Cloud DB)
Generated by: Wave 5.16e2 implementor (no src/ changes; two docs-scoped helper scripts under `docs/recordings/smoke-run-16/scripts/` only).

## Named gap (the reason for the ⚠️ verdict)

`services/api/src/bootstrap.ts:34-64` (`buildFrtbSnippets`) builds **six** snippets — Delta + Vega for GIRR, EQUITY, FX — and `bootstrap.ts:90-91` loads only that array via `loadFrtbLibrary`. The three Curvature snippet builders (`services/calc/src/girrCurvatureSnippet.ts:39`, `…/equityCurvatureSnippet.ts:31`, `…/fxCurvatureSnippet.ts:30`) and their Lua kernels (`services/calc/lib/*_curvature.lua`) shipped in Wave 5.16a/b (commits `7171c6b` + `1c48d97`) and Wave 5.16d (commit `5c8373d`) added generator + test coverage, but neither wave extended `buildFrtbSnippets` to include the curvature snippets. The runtime symptom is `FCALL girr_curvature … → (error) ERR Function not found` on the live api; `FUNCTION LIST` confirms only `{sbm_delta_bucket, sbm_vega_bucket, equity_delta, equity_vega, fx_delta, fx_vega}` are registered.

Workaround used this wave (docs-scoped, no src/ change): `docs/recordings/smoke-run-16/scripts/load-curvature-frtb.ts` imports the existing public snippet builders (Delta+Vega via `buildFrtbSnippets`, Curvature via the three `build*CurvatureSnippet` builders) with ρ_curv = (ρ_delta)² per §21.5(3), then calls the existing `loadFrtbLibrary` helper to atomically replace the `frtb` library on every master with all 9 functions. Follow-up wave is expected to lift the same three lines into `buildFrtbSnippets`.

## Pre-flight (blocking gates)

| # | Gate | Verdict | Evidence |
|--:|------|:-------:|----------|
| 1 | `.env.local` has `REDIS_CLUSTER=FALSE` AND `REDIS_TLS=FALSE` | ✅ | Both values present (host-side `grep`; values not echoed). |
| 2 | Standalone Redis Cloud DB reachable, `DBSIZE=0` after FLUSHALL | ✅ | `scripts/smoke-reset-cluster.sh --yes` logs `used_memory_human before/after: 7.70M / 7.70M`, `XLEN sensitivities:in before/after: 5 / 0`, and `FT._LIST sanity: OK`. Direct `redis-cli DBSIZE → 0` afterwards. |
| 3 | `services/generator/src/row-generator.ts` supports `sensitivityTypes: ["Curvature"]` | ✅ | Wave 5.16d (commit `5c8373d`) added the per-leg branch and the tests; verified by running the docs-scoped `ingest-typed.ts --types Curvature` smoke (5 rows) before the real run. |

## Per-step verdict

| Step | Action | Verdict | Notes |
|---:|--------|:-------:|-------|
| 1 | `FLUSHALL` via `scripts/smoke-reset-cluster.sh --yes` + capture B | ✅ | `logs/used-memory-baseline.json`: `used_memory_bytes=8 062 320` / `used_memory_human=7.68M`. `DBSIZE=0` confirmed. |
| 2 | `docker compose down -v` + `up -d --build --wait` | ✅ | All six containers (api / calc / ingest / loadgen / source / ui) report `Healthy`. |
| 3 | Capture B' (post bootstrap, pre-ingest) | ✅ | `logs/used-memory-post-bootstrap.json`: `used_memory_bytes=8 250 592` / `used_memory_human=7.86M`. `DBSIZE=1` (idx:sens definition + stream group state). |
| 4 | Ingest 2 000 Delta + 2 000 Vega + 2 000 Curvature via `ingest-typed.ts` (3 invocations) | ✅ | `logs/ingest-Delta.json` (1 198 rps), `logs/ingest-Vega.json` (1 335 rps), `logs/ingest-Curvature.json` (1 271 rps). Per-leg class mix = GIRR 667 / EQUITY 667 / FX 666. |
| 5 | Wait for `idx:sens.num_docs == 6 000` | ✅ | Already at 6 000 at first poll (t=1s). `XLEN sensitivities:in = 6 000`, `XPENDING = 0`, `num_docs = 6 000`. `logs/ft-info-post-ingest.txt`. |
| 6 | Capture P + HARD GUARD vs 30 MB Δ cap | ✅ | `logs/used-memory-post-ingest.json`: `used_memory_bytes=31 937 536` / `used_memory_human=30.45M`. **P − B' = 23 686 944 B = 22.59 MB = 75.32 % of the 30 MB Δ cap** → guard PASS. |
| 7 | Per-`sensitivity_type` FT.SEARCH count check (2 000 each) | ✅ | `FT.SEARCH idx:sens @sensitivity_type:{Delta\|Vega\|Curvature} LIMIT 0 0` → `Delta: 2000`, `Vega: 2000`, `Curvature: 2000`. |
| 8 | First-pass calc matrix (9 POSTs) | ⚠️ | 6/9 HTTP 200; 3/9 (`*_curvature`) returned HTTP 500 `ERR Function not found`. Named gap captured above. |
| 9 | Runtime helper to load curvature snippets | ✅ | `docs/recordings/smoke-run-16/scripts/load-curvature-frtb.ts` ran `FUNCTION LOAD REPLACE` with the full 9-snippet bundle. `FUNCTION LIST` afterwards shows all 9 functions registered. |
| 10 | Re-run 9-variant calc matrix + save 9 raw JSONs | ✅ | 9/9 HTTP 200; all charges strictly positive; every per-bucket entry has `count > 0`. Files: `calc/calc-{GIRR,EQUITY,FX}-{Delta,Vega,Curvature}.json` + `aggregate.json`. |
| 11 | Capture final used_memory post-calc | ✅ | `logs/used-memory-final.json`: `used_memory_bytes=31 941 088` / `used_memory_human=30.46M` (Δ vs B' = 22.59 MB; calc fan-out cost = +3 552 B, well inside noise). |
| 12 | Extend `docs/demo/mar21-traceability.md` with §21.5 Curvature clauses (verified: no `TBD`, no `Nxx`) | ✅ | New §21.5 section with clauses (2), (3), (5), (5)(b); Live column carries this run's GIRR Curvature charge 9 597.031715206407 + per-bucket K_b/S_b/count for the three highest buckets and aggregate stats; provenance + footer updated. |

## `used_memory` timeline (4-point, single shard, single standalone DB)

| Checkpoint | `used_memory_human` | `used_memory_bytes` | Δ-over-B' | % of 30 MB Δ cap |
|------------|---------------------|--------------------:|----------:|-----------------:|
| Baseline B (post FLUSHALL, pre compose-up) | 7.68 M | 8 062 320 | — | — |
| B' (post compose-up + `bootstrap:ready`, pre-ingest) | 7.86 M | 8 250 592 | 0 B | 0 % |
| Post-ingest P (6 000 rows: 2 000 each Delta/Vega/Curvature) | **30.45 M** | **31 937 536** | **23 686 944 B = 22.59 MB** | **75.32 %** ✅ |
| Final (post 9-variant calc matrix) | 30.46 M | 31 941 088 | 23 690 496 B = 22.59 MB | 75.33 % |

**Per-row cost (measured this run)**: 23 686 944 B / 6 000 rows = 3 948 B/row = **3.86 KB/row**. Curvature rows carry the `{cvr_up, cvr_down}` shape A pair (GIRR: per-tenor arrays, Equity/FX: scalars) so the per-row cost is broadly aligned with the Delta+Vega blended cost from smoke-run-15 (3.72 KB/row). Δ-over-B' headroom = 30 MB − 22.59 MB = **7.41 MB** under the 30 MB cap.

## Δ vs 30 MB Δ-over-baseline cap

**Peak Δ (P − B') = 23 686 944 B = 22.59 MB = 75.32 % of the 30 MB cap → 🟢 GREEN (7 412 416 B = 7.07 MB of headroom).** No HARD GUARD trip at step 6. The headroom is consistent with the smoke-run-16 preflight projection (9.47 KB/row × 2 000 Curvature = 18.49 MB plus 7.27 MB Delta+Vega baseline = 25.76 MB) — actual usage came in 3.17 MB under the projection because the allocator amortises the JSON header overhead across the three legs sharing the same `sens:{risk_class:bucket}:*` hash-tag prefix.

## Calc matrix (9-variant evidence)

| risk_class | leg       | HTTP | wallclock | charge                  | per_bucket count | non-zero buckets | total_ms | fanout_ms |
|------------|-----------|:----:|----------:|------------------------:|-----------------:|-----------------:|---------:|----------:|
| GIRR       | Delta     | 200  |   398 ms  | 0.6964748186716158      | 11 | 11 / 11 | 296.497 | 173.500 |
| GIRR       | Vega      | 200  |   403 ms  | 66.95744760686635       | 11 | 11 / 11 | 306.319 | 182.542 |
| GIRR       | Curvature | 200  |   411 ms  | **9 597.031715206407**  | 11 | 11 / 11 | 314.266 | 191.024 |
| EQUITY     | Delta     | 200  |   384 ms  | 7.43159463086323        | 13 | 13 / 13 | 284.812 | 159.470 |
| EQUITY     | Vega      | 200  |   380 ms  | 15.992429861315175      | 13 | 13 / 13 | 281.085 | 158.573 |
| EQUITY     | Curvature | 200  |   382 ms  | **344.3317327977044**   | 13 | 13 / 13 | 285.536 | 162.161 |
| FX         | Delta     | 200  |   312 ms  | 1.4255541516885033      | 11 | 11 / 11 | 263.580 | 143.919 |
| FX         | Vega      | 200  |   333 ms  | 12.891396385264315      | 11 | 11 / 11 | 270.456 | 148.266 |
| FX         | Curvature | 200  |   372 ms  | **1 095.1081812019447** | 11 | 11 / 11 | 276.181 | 149.055 |

L2 risk-class totals (`√(Delta² + Vega² + Curvature²)`): **GIRR = 9 597.27**, EQUITY = 344.51, FX = 1 095.18 (see `aggregate.json` for the canonical figures). Grand-total L2 across risk classes = **9 665.70**. Curvature charges dwarf Delta/Vega because the §21.5(2) CVR shift uses the price-impact PnL — bp-scale shocks revalued at portfolio bp/bp — so the absolute scale is ~3 orders of magnitude larger than the Delta charge per row, as expected. Per-variant wallclock 312–411 ms; fanout 144–191 ms; total_ms 264–314 ms.

## Repo hygiene

- `git diff --name-only` services/: zero entries (no src/ change). The two new files under `docs/recordings/smoke-run-16/scripts/` (`ingest-typed.ts`, `load-curvature-frtb.ts`, `build-aggregate.mjs`) are docs-scoped helpers that import — but do not modify — existing public exports from `@frtb/calc`, `@frtb/schema`, `@frtb/redis-client`, and the `services/generator/src/*` row generator + producer.
- Secrets policy: `REDIS_URL` / password / username are loaded from `.env.local` via `export $(grep -v '^#' .env.local | xargs)` and never echoed in command argv, script stdout, or any saved JSON. The `scripts/smoke-reset-cluster.sh` warning about `-a` / `-u` on the CLI is a redis-cli line-noise artefact, not a leak (URL passes via `-u` not `-a`).

## Outcome classification

**⚠️ DEGRADED.** The Curvature legs of the matrix require a one-off runtime helper (`load-curvature-frtb.ts`) to register the `{girr,equity,fx}_curvature` Redis Functions until `services/api/src/bootstrap.ts:buildFrtbSnippets` is extended to include them. Once the bootstrap is updated (the natural follow-up wave), the same 9-variant matrix will work end-to-end on `docker compose up -d --wait` alone. All other Definition-of-Done items are green: 9/9 calc HTTP 200 with strictly positive charges, every per-bucket entry has `count > 0`, P − B' = 22.59 MB < 30 MB cap, the 9 raw `calc-*.json` files + `aggregate.json` are saved verbatim, and `docs/demo/mar21-traceability.md` carries the full §21.5(2)–(5)(b) clause-by-clause mapping with this run's live values (no `TBD`, no `Nxx`).

> **Bootstrap gap closed in Wave 5.16f1**: `services/api/src/bootstrap.ts:buildFrtbSnippets` now registers all 9 Redis Functions at startup. The one-off `load-curvature-frtb.ts` helper from this run is no longer needed; a clean-room `docker compose up` would produce the same 9/9 matrix without manual intervention. **Effective verdict: 🟢 GREEN.**
