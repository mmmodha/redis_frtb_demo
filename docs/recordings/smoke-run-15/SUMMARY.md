# Wave 5.15q — Smoke Re-run #15 — SUMMARY

Run date: 2026-05-29
Generated by: Wave 5.15q implementor (prove MVP end-to-end against new standalone Redis Cloud DB; produce MAR21 §21.4 traceability pack)
Target Redis: `lip-veil-spring-32424.db.redis.io:14596` — standalone Redis Cloud DB (Redis 8.4.0, `redis_mode=standalone`, single primary + 1 replica, `maxmemory_policy=noeviction`).

## Verdict: 🔴 RED — api bootstrap blocked at step 2 (redis-ready timeout). No calc matrix executed. DEBUG-NEEDED.

The host-side connectivity probe (pre-flight gate 2) **passed** end-to-end against the new standalone Redis Cloud DB: `connect.ok=true`, `ping.ok=true`, `write_path.ok=true`, `mode=standalone`, `redis_mode=standalone`, baseline `used_memory_human=4.25 M`. The endpoint itself is healthy and writable from this host. **However**, when the api container is brought up under `docker compose up -d --build --wait`, `services/api/src/index.ts:76` calls `ensureRedisReady(redis, { hasUrl: true })` and the cluster-mode branch in `services/api/src/redis-ready.ts:54-77` waits for ioredis `'ready'` event on a client built with `createRedisClient({ lazyConnect: true, … })`. With `REDIS_CLUSTER=false` (uppercase `FALSE` in `.env.local`, normalised via `envBool` at `shared/redis-client/src/index.ts:52-57`), the client is a standalone `Redis`, not a `Cluster`; standalone + `lazyConnect:true` does not auto-connect, so `'ready'` never fires. The 30 000 ms bound (the new `REDIS_READY_TIMEOUT_MS=30000`) elapses and the gate emits `redis-unreachable` — `/healthz` returns 503 indefinitely, the `--wait` flag trips "container is unhealthy", and the rest of the stack never moves out of `Created`. This is a code path that needs editing in `services/api/src/redis-ready.ts` (call `.connect()` on the standalone client even when `hasUrl=true`, or pass cluster-vs-standalone through `EnsureRedisReadyOptions`) and is **out of scope** for this wave (`services/*/src/` edits are explicitly forbidden by the DoD).

Per the task's RED-outcome gate ("STOP, report, capture api/calc container logs to `docs/recordings/smoke-run-15/logs/`, do NOT retry"), procedure halted after step 2. No ingest, no calc, no live numbers from this run. The MAR21 §21.4 traceability pack at `docs/demo/mar21-traceability.md` is still delivered, with file:line refs to the current source tree and **Live value** rows populated from the most recent successful 6-variant matrix (smoke-run-14, attempt 2 at `--rows 150000`) and explicitly annotated as "reference values — this run blocked at step 2; see SMOKE-RUN-15 SUMMARY".

## Pre-flight gate verdict

| Gate | Required | Observed | Verdict |
|------|----------|----------|:-------:|
| 1a   | `.env.local` contains `REDIS_CLUSTER=false` | `REDIS_CLUSTER=FALSE` (uppercase) — value parses to `false` via `envBool().toLowerCase()` at `shared/redis-client/src/index.ts:52-57` | ✅ (case-insensitive match) |
| 1b   | `.env.local` contains `REDIS_READY_TIMEOUT_MS=30000` | present, exact value | ✅ |
| 2    | `connectivity-probe.mjs` reports `connect.ok=true, write_path.ok=true, mode=standalone` | `logs/connectivity-probe-preflight.json` — all three true; mode=standalone; baseline `used_memory_human=4.25 M` (~4 458 480 bytes) | ✅ |

Both blocking gates passed. The failure is downstream, in the api container's redis-readiness path, not in connectivity to the DB.

## Per-step verdict

| Step | Action | Verdict | Notes |
|-----:|--------|:-------:|-------|
| 1 | Pre-flight (gates 1 + 2) | ✅ | See table above. |
| 2 | `docker compose down -v` + `up -d --build --wait` | 🔴 | api never reaches `bootstrap-ok`; `/healthz → {"status":"bootstrap-failed","reason":"redis-unreachable"}`; api logs: `{"service":"api","status":"redis-unreachable","target":"env:REDIS_URL","err":"Error: redis-ready timeout after 30000ms"}`. `logs/api-bootstrap.log`, `logs/healthz-failed.json`, `logs/compose-ps-failed.txt`. |
| 3 | Capture `used_memory` baseline (host probe) | ✅ | `logs/connectivity-probe-preflight.json` → `used_memory_bytes=4458480` (≈ 4.25 M); the Redis Cloud DB carries module/index overhead at baseline. |
| 4 | Generator: `--rows 2500 --classes GIRR,EQUITY,FX` | ⏸ skipped | Cannot run while api is unhealthy; `generator` container stayed in `Created` and was never started. Per the RED gate, no retry. |
| 5 | `sleep 10` | ⏸ skipped | n/a — no ingest happened. |
| 6 | Capture `used_memory` post-ingest + 10 MB cap guard | ⏸ skipped | No ingest occurred, so no post-ingest snapshot. **Pre-teardown** probe (`logs/connectivity-probe-pre-teardown.json`) confirms `used_memory_human=4.25 M`, identical to baseline — peak well under the 10 MB cap. |
| 7 | `FT.INFO idx:sens` density gate | ⏸ skipped | n/a — bootstrap never ran, so `idx:sens` was never created. |
| 8 | 6-variant calc matrix via `scripts/run-calc.sh` | ⏸ skipped | n/a — api unhealthy, port 8080 returns 503. |
| 9 | Capture `used_memory` pre-teardown | ✅ | `logs/connectivity-probe-pre-teardown.json` → `used_memory_bytes=4458480` (≈ 4.25 M), unchanged from baseline. |
| 10 | `docker compose down -v` | ✅ | `logs/compose-down-final.log` — clean teardown, volume removed. |
| 11 | Write SUMMARY.md | ✅ | this file. |
| 12 | Write `docs/demo/mar21-traceability.md` | ✅ | clauses 3, 4, 5, 5(b) populated with file:line refs against the current source tree; **Live value** column populated from smoke-run-14 GIRR Delta reference numbers with explicit annotation that this run was blocked at step 2. |

## Calc matrix — DEBUG-NEEDED

No live `POST /calc/sbm` calls executed this run (step 8 skipped, RED gate). The 6 expected output files `docs/recordings/smoke-run-15/calc-{girr,equity,fx}-{delta,vega}.json` are **not present** — the task instruction "save 6 raw JSONs BEFORE writing SUMMARY so if anything blows up we still have evidence" implicitly requires step 8 to have run. The latest successful 6-variant matrix is `docs/recordings/smoke-run-14/calc-results.json` (attempt 2, `--rows 150000`, single-shard cohort on shard B); summary: 6/6 HTTP 200, all charges non-zero, GIRR Delta = 3.114835551, GIRR Vega = 162.6063901, EQUITY Delta = 35.72550084, EQUITY Vega = 68.30686423, FX Delta = 3.081043259, FX Vega = 9.342457450 (full bodies in that prior run's `calc-results.json`). The 5.15q workload would have been smaller (2 500 rows vs 150 000) and on a different DB, so absolute charges will differ — but the per-bucket K_b > 0 / S_b ≠ 0 / count > 0 shape would be identical.

## `used_memory` observations vs 10 MB cap

| Snapshot         | `used_memory_human` | `used_memory_bytes` | vs 10 × 1024 × 1024 B cap | Source                                                |
|------------------|---------------------|---------------------|----------------------------|--------------------------------------------------------|
| Pre-flight (gate 2) | 4.25 M           | 4 458 480           | **42.5 % of cap**          | `logs/connectivity-probe-preflight.json`              |
| Pre-teardown      | 4.25 M           | 4 458 480           | **42.5 % of cap**          | `logs/connectivity-probe-pre-teardown.json`           |

Peak `used_memory_bytes` observed during the run window: **4 458 480 B** ≈ 4.25 M. Under the 10 MB cap by a comfortable margin (would not have triggered `BUDGET-EXCEEDED` even if ingest had run — projected 2 500-row footprint is < 1 MB on top of the 4.25 M module/index baseline).

## Outcome classification: 🔴 RED

- `POST /calc/sbm` never reachable this run — every call would have returned 503 due to bootstrap-failed.
- DoD not met: SUMMARY exists, but 0/6 calc JSONs present; charges field n/a; peak memory well under cap (the one bright spot).
- Traceability pack delivered with file:line refs against the live source tree and reference live values from smoke-run-14 (explicitly annotated).
- **Recommendation**: a follow-up wave should patch `services/api/src/redis-ready.ts` so the `hasUrl=true` path also handles standalone clients (e.g. call `.connect()` and tolerate "already connecting" the same way the standalone branch does today; or thread `cluster: boolean` through `EnsureRedisReadyOptions` so the api's `index.ts` can route to the correct branch based on `process.env.REDIS_CLUSTER` rather than presence-of-URL). Until that lands, the demo cannot run against a standalone Redis Cloud DB even though the DB itself is healthy from outside the api container.

## Repo hygiene

- `git diff --name-only services/ | grep -E "services/.+/src/"` → **zero entries** (no production-code edits this wave; the three `services/*/package.json` lines in `git status` are pre-existing changes carried in from earlier waves).
- Secrets clean: `REDIS_URL` value, password value, and url-decoded credentials are not echoed in any file under `docs/recordings/smoke-run-15/` or `docs/demo/`. The api-bootstrap.log was scrubbed with `sed -E 's#(redis(s)?://[^@[:space:]"]*@)#redis://REDACTED@#g'` before being written. The connectivity-probe JSONs already redact by construction (host:port only from ioredis options, never re-serialised from the URL).

## Artefacts captured

```
docs/recordings/smoke-run-15/
  SUMMARY.md                                        (this file)
  scripts/                                          (unchanged from delegation kit; reused as-is)
    connectivity-probe.mjs, run-calc.sh, ft-info.mjs, sample-sens-key.mjs,
    shard-memory.mjs, per-shard-data-distribution.mjs, per-shard-scan.mjs,
    image-freshness.sh, measure-handshake.mjs
  logs/
    connectivity-probe-preflight.json               pre-flight gate 2 (PASS; baseline 4.25 M)
    connectivity-probe-pre-teardown.json            step 9 (4.25 M, unchanged)
    api-bootstrap.log                               api container logs (RED diagnostic; scrubbed)
    calc-bootstrap.log                              calc container logs (empty — never started)
    compose-ps-failed.txt                           step 2 final container states (api=unhealthy)
    healthz-failed.json                             /healthz at step 2 (503 + bootstrap-failed)
    compose-down-final.log                          step 10 teardown
```
