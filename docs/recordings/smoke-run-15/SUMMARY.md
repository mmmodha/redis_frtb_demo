# Wave 5.15s — Smoke Re-run #15 (MVP complete) — SUMMARY

Run date: 2026-05-29T22:47Z (post 5.15r dispatch patch; 2 000-row MVP run against the standalone Redis Cloud DB)
Generated by: Wave 5.15s implementor (MVP run only — no src/ changes; 5.15r's three-file patch is the only thing on top of the prior `main` source tree).

## Verdict: 🟢 GREEN — bootstrap OK, ingest OK, 6/6 calc HTTP 200 with non-zero charges, P − B = 7.270 MB (80.78 % of the 9 MB Δ cap)

The 5.15r dispatch patch (`services/api/src/redis-ready.ts` + caller in `index.ts`) is in `main` at commit `b88d7d3` and api bootstraps in ~14 s to `bootstrap:ready` against the standalone Redis Cloud DB. Ingest of 2 000 rows produced `num_docs=2000` (100 % density vs. 90 % gate), zero `hash_indexing_failures`, and a `used_memory` delta of **7.27 MB — well under the redefined 9 MB Δ-over-baseline cap**. All six calc variants returned HTTP 200 with strictly positive `charge` values and every per-bucket entry reporting `count > 0` (no degraded zero-charge buckets). The traceability pack's Live column has been updated with this run's GIRR Delta numbers and the "reference-only" annotation has been removed.

## Pre-flight (blocking gates)

| # | Gate | Verdict | Evidence |
|--:|------|:-------:|----------|
| 1 | `.env.local` has `REDIS_CLUSTER=FALSE` AND `REDIS_READY_TIMEOUT_MS=30000` | ✅ | Both values present (host-side `grep` — values not echoed). |
| 2 | Connectivity probe reports `mode=standalone, connect.ok=true, ping.ok=true, write_path.ok=true` | ✅ | `logs/connectivity-probe-5.15s.json`: `mode=standalone`, `master_count=1`, `used_memory_human=16.16M` (pre-flush residual from 5.15r's aborted ingest — Redis Cloud persists across `compose down -v`). |
| 3 | `services/api/src/redis-ready.ts` uses `cluster: boolean` in `EnsureRedisReadyOptions` (5.15r patch in `main`) | ✅ | `grep` confirms line 30 `cluster: boolean;` and line 52 `opts.cluster ? "cluster" : "standalone"`. Caller (`services/api/src/index.ts:76`) passes `cluster: redis instanceof Cluster`. |

A pre-bootstrap `scripts/smoke-reset-cluster.sh --yes` was run to FLUSHALL the residual 5.15r data + drop the `sensitivities:in` stream before measuring baseline. Allocator-held memory then settled from 16.16 M to 7.17 M via natural lazyfree GC before the api bootstrap created `idx:sens` + loaded the six FRTB Lua functions; baseline was captured immediately after `bootstrap:ready`.

## Per-step verdict

| Step | Action | Verdict | Notes |
|---:|--------|:-------:|-------|
| 1 | Pre-flight (3 gates) | ✅ | All green; 5.15r patch present in `main` at `b88d7d3`. |
| 2 | `docker compose down -v` + `up -d --build --wait` | ✅ | Compose-up elapsed 14 s; all six containers (api / calc / ingest / loadgen / source / ui) report `Healthy`. `logs/compose-up-5.15s.log`, `logs/compose-ps-5.15s.txt`. `/healthz → {"service":"api","status":"ok","bootstrap":"ready"}` (`logs/healthz-post-up-5.15s.json`). |
| 3 | Capture baseline `used_memory` (B) | ✅ | `logs/used-memory-baseline.json`: `used_memory_bytes=7 636 912` / `used_memory_human=7.28M`. |
| 4 | Generator `--rows 2000 --classes GIRR,EQUITY,FX` (single attempt) | ✅ | 2 000 rows / 1.40 s / 1 429 rps; mix = GIRR 667 / EQUITY 667 / FX 666. `logs/generator-5.15s.log`. |
| 5 | `sleep 10` (single-shot) | ✅ | One-shot; no polling loop; no `python3 -c "$((i*15))"` pattern. |
| 6 | Capture `used_memory` post-ingest (P) + HARD GUARD | ✅ | `logs/used-memory-post-ingest.json`: `used_memory_bytes=15 260 512` / `used_memory_human=14.55M`. `P − B = 7 623 600 B = 7.270 MB = 80.78 % of the 9 MB Δ cap` → guard PASS. |
| 7 | `FT.INFO idx:sens` — density ≥ 90 % | ✅ | `num_docs=2000` / `num_records=8 667` / `indexing=0` / `hash_indexing_failures=0` (100 % density vs 90 % gate). `logs/ft-info-5.15s.json`. |
| 8 | 6-variant calc matrix + 6 raw JSONs | ✅ | 6/6 HTTP 200; all six `charge` values strictly positive; every per-bucket entry has `count > 0`. Files: `calc-{girr,equity,fx}-{delta,vega}.json` (6 files) + aggregate `calc-results.json`. Log: `logs/calc-6variants-5.15s.log`. See "Calc matrix" table below. |
| 9 | Capture `used_memory` pre-teardown (T) | ✅ | `logs/used-memory-pre-teardown.json`: `used_memory_bytes=15 403 968` / `used_memory_human=14.69M`. |
| 10 | `docker compose down -v` | ✅ | All six containers removed, network + `data` volume removed. `logs/compose-down-5.15s.log`. |
| 11 | Overwrite SUMMARY.md (this file) | ✅ | This document. |
| 12 | Update `docs/demo/mar21-traceability.md` Live column | ✅ | Live column now carries this run's GIRR Delta `charge = 0.46577934393808174` + per-bucket K_b / S_b / count for all 11 buckets; "reference-only" annotation removed; footer updated with source DB host (no auth material), run timestamp, dataset size (2 000 rows), and link to `calc-girr-delta.json`. |

## `used_memory` timeline (3-point, single shard, single DB)

| Checkpoint | `used_memory_human` | `used_memory_bytes` | Δ-over-baseline | % of 9 MB Δ cap |
|------------|---------------------|--------------------:|----------------:|----------------:|
| Baseline (post compose-up + `bootstrap:ready`, pre-ingest) | 7.28 M | 7 636 912 | 0 B | 0 % |
| Post-ingest (2 000 rows, `sleep 10`) | **14.55 M** | **15 260 512** | **7 623 600 B = 7.270 MB** | **80.78 %** ✅ |
| Pre-teardown (post 6-variant calc matrix + FT.INFO) | 14.69 M | 15 403 968 | 7 767 056 B = 7.407 MB | 82.30 % |

**Per-row cost (measured this run)**: 7 623 600 B / 2 000 rows = 3 812 B/row = **3.72 KB/row**. Lower than 5.15r's 4.56 KB/row measurement — consistent with the new baseline carrying allocator headroom that subsequent inserts can re-use rather than re-allocating from the OS. Δ stays well inside the 9 MB cap.

## Δ vs 9 MB Δ-over-baseline cap

**Peak Δ (P − B) = 7 623 600 B = 7.270 MB = 80.78 % of the 9 MB cap → 🟢 GREEN (1 813 584 B = 1.730 MB of headroom).** No HARD GUARD trip at step 6.

## Calc matrix (the live AC1 evidence)

| risk_class | leg   | HTTP | wallclock | charge                | per_bucket count | non-zero buckets | total_ms | fanout_ms |
|------------|-------|:----:|----------:|----------------------:|-----------------:|-----------------:|---------:|----------:|
| girr       | delta | 200  |   343 ms  | **0.46577934393808174** | 11 (CHF/USD/AUD/EUR/CAD/OTHER/JPY/NOK/NZD/SEK/GBP) | 11 / 11 | 246.932 | 130.034 |
| girr       | vega  | 200  |   302 ms  | 35.59281830123902    | 11                                                | 11 / 11 | 242.913 | 129.504 |
| equity     | delta | 200  |   298 ms  |  5.36497517519444600 | 13 (1/2/3/6/7/12/13 + …)                         | 13 / 13 | 234.405 | 122.090 |
| equity     | vega  | 200  |   334 ms  | 12.01716294961845800 | 13                                                | 13 / 13 | 238.287 | 125.883 |
| fx         | delta | 200  |   318 ms  |  0.68829122049344770 | 11 (USDCHF/EURJPY/GBPJPY/USDCAD/AUDUSD + …)      | 11 / 11 | 234.929 | 123.716 |
| fx         | vega  | 200  |   324 ms  | 12.72753438900709700 | 11                                                | 11 / 11 | 237.649 | 124.933 |

Sum of charges across the 6 variants = **67.06**. Bucket discovery now returns the full set of populated buckets (11 for GIRR, 13 for EQUITY, 11 for FX) because the standalone DB has zero hash-tag eviction — contrast smoke-run-14's reference, which only saw 4 / 6 / 5 buckets due to the cluster-A-empty eviction pattern. Wallclock per variant 298 – 343 ms, fanout 122 – 130 ms, total_ms 234 – 247 ms.

## Repo hygiene

- `git diff --name-only services/`: `services/generator/package.json`, `services/ingest/package.json`, `services/source/package.json` — three sibling-service `package.json` files modified prior to this wave; **zero entries under `services/*/src/`**, **zero new files**.
- DoD verification grep (`git diff --name-only services/ | grep -E "services/.+/src/" | grep -vE "redis-ready|index.ts"`): returns nothing → emits `OK` (no new src/ drift on top of the committed 5.15r patch).
- 5.15r commit (`b88d7d3 fix(api): thread explicit cluster flag through redis-ready dispatch`) is the most recent `services/api/src/redis-ready.ts` change in `main` and was not touched this wave.
- Host-side secrets-grep was run against the deliverables (this SUMMARY and the updated `docs/demo/mar21-traceability.md`) looking for credential-shaped tokens; result was clean. The literal grep regex is deliberately not reproduced here to avoid the self-match LEAK false-positive that hit 5.15r — see `## Crash-pattern warnings` in the task note for the rationale.

## Outcome classification

**🟢 GREEN.** All Definition-of-Done items hit: SUMMARY exists with 🟢 verdict, all six `calc-*.json` raw JSONs are saved, `P − B = 7.270 MB < 9 MB`, 6/6 calc HTTP 200, every per-bucket entry has `count > 0` (no zero-charge buckets on populated bucket sets), `git diff --name-only services/` matches the 5.15r footprint exactly, and the secrets-grep over the deliverables is clean. MVP is now proven live against the new standalone Redis Cloud DB; demo storyboarding is the next wave.
