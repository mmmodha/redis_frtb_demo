# Wave 5.15r — Smoke Re-run #15 — SUMMARY

Run date: 2026-05-29T22:36Z (post-patch re-run on the new standalone Redis Cloud DB)
Generated by: Wave 5.15r implementor (surgical api/redis-ready dispatch fix + MVP re-attempt)

## Verdict: 💰 BUDGET-EXCEEDED — bootstrap GREEN, ingest GREEN, then HARD-GUARD trip at step 9

The Wave 5.15r patch landed cleanly and unblocked the Wave 5.15q diagnosis: api now boots all the way to `bootstrap:ready` against the new standalone Redis Cloud DB. The 2,500-row ingest completed, FT.INFO reports `num_docs=2500` (100 % density, vs. the 90 % gate). However, **post-ingest `used_memory` measured 15.84 MB**, which exceeds the user-directive HARD CAP of 10 MB. Per the task's outcome classification ("💰 BUDGET-EXCEEDED: peak `used_memory` > 10 MB → abort cleanly, report"), the 6-variant calc matrix was NOT executed and `docs/demo/mar21-traceability.md` was NOT updated (the Live column still carries the smoke-run-14 reference values from the 5.15q completion).

## Pre-flight (blocking gates)

| # | Gate | Verdict | Evidence |
|--:|------|:-------:|----------|
| 1 | `.env.local` has `REDIS_CLUSTER=FALSE` AND `REDIS_READY_TIMEOUT_MS=30000` | ✅ | Both values present (host-side `grep` — value not echoed). |
| 2 | Connectivity probe reports `connect.ok=true, write_path.ok=true, mode=standalone` | ✅ | `redis_mode=standalone`, `connect.ok=true`, `write_path.ok=true`, baseline `used_memory_human=4.70M`. |
| 3 | Read 5.15q completion report for diagnosis context | ✅ | Confirmed root cause: dispatch keyed off `hasUrl` not the cluster flag → standalone client with lazyConnect:true never fired `'ready'` → 30 s timeout. |

## Per-step verdict

| Step | Action | Verdict | Notes |
|---:|--------|:-------:|-------|
| 1 | Pre-flight | ✅ | All 3 gates green. |
| 2 | Patch `redis-ready.ts` + caller in `index.ts` | ✅ | Approach: thread explicit `cluster:boolean` through `EnsureRedisReadyOptions`; caller uses `redis instanceof Cluster` for the discriminator. See "Patch summary". |
| 3 | Unit-test both branches in `redis-ready.test.ts` | ✅ | Added regression test "with cluster=false + lazyConnect, calls .connect() (no 'ready' wait)"; existing cluster + standalone tests renamed to use `cluster:` instead of `hasUrl:`. |
| 4 | `pnpm --filter @frtb/api test` | ✅ | **125 passed / 3 skipped** across 22 files. redis-ready.test.ts: **11 / 11 green**. |
| 5 | `docker compose down -v` + `up -d --build --wait` | ✅ | api healthcheck reaches `bootstrap:ready` (`/healthz → {"service":"api","status":"ok","bootstrap":"ready"}`); api logs show `bootstrap:idx:sens action=created` and `bootstrap:frtb action=loaded functions=[sbm_delta_bucket, sbm_vega_bucket, equity_delta, equity_vega, fx_delta, fx_vega]`. ALL six containers (api, calc, ingest, ui, source, loadgen) report Healthy. |
| 6 | Capture baseline `used_memory` | ✅ | `used_memory_human=4.70M` / `used_memory_bytes=4 929 888`. |
| 7 | Generator `--rows 2500 --classes GIRR,EQUITY,FX` (single attempt) | ✅ | 2 500 rows / 1.68 s / 1 491 rps; mix = GIRR 834 / EQUITY 833 / FX 833. `logs/generator.log`. |
| 8 | `sleep 10` (single-shot) | ✅ | One-shot, no polling loop, no `python3 -c "$((i*15))"` pattern. |
| 9 | Capture `used_memory` post-ingest + HARD GUARD | 💰 | **`used_memory_human=15.84M` / `used_memory_bytes=16 610 464` → BUDGET-EXCEEDED (158.4 % of 10 MB cap)**. Run aborted here per the task's HARD-GUARD instruction. |
| 10 | `FT.INFO idx:sens` (cheap evidence capture outside the abort path) | ✅ | `num_docs=2500` (100 % density, vs. 90 % gate), `num_records=10 834`, `indexing=0`, `hash_indexing_failures=0`. |
| 11 | 6-variant calc matrix | ⏸ | NOT EXECUTED — HARD-GUARD abort at step 9. |
| 12 | Capture pre-teardown `used_memory` | ✅ | `used_memory_human=15.88M` / `used_memory_bytes=16 656 688`. |
| 13 | `docker compose down -v` | ✅ | All six containers removed, network + data volume removed. |
| 14 | Write SUMMARY.md | ✅ | This file. |
| 15 | Update `mar21-traceability.md` Live column | ⏸ | NOT EXECUTED — no live calc numbers were produced this run. Reference-only annotation from 5.15q remains in place; supervisor decides whether the next wave changes the cap, the dataset size, or both before this can be populated live. |

## Patch summary

Three files, 36 insertions / 20 deletions:

```
 services/api/src/index.ts              |  4 ++--
 services/api/src/redis-ready.ts        | 10 ++++----
 services/api/tests/redis-ready.test.ts | 42 ++++++++++++++++++++++------------
 3 files changed, 36 insertions(+), 20 deletions(-)
```

- `services/api/src/redis-ready.ts`: rename `EnsureRedisReadyOptions.hasUrl` → `cluster`; dispatch now keys off `opts.cluster` instead of `opts.hasUrl`. Comment block updated to reflect the corrected contract.
- `services/api/src/index.ts`: value-import `Cluster` from ioredis (was type-only); caller passes `cluster: redis instanceof Cluster` instead of `hasUrl: !!process.env.REDIS_URL`.
- `services/api/tests/redis-ready.test.ts`: rename all 8 call sites `hasUrl:` → `cluster:`; add 1 new regression test covering the explicit "cluster=false + has-URL + lazyConnect → calls .connect()" path that was previously misrouted.

**Approach choice & rationale**: the task offered two approaches — (a) thread an explicit `cluster:boolean` through the options, or (b) have the standalone branch tolerate `hasUrl=true` by calling `.connect()`. **Chose (a)** because it makes the dispatch contract self-documenting at the call site (no implicit "this option's name lies about what it actually means"), keeps a single source of truth for the cluster discriminator (`redis instanceof Cluster` post-construction, computed from the same value that gated the constructor choice itself), and adds zero conditional branches inside `ensureRedisReady` beyond renaming a field. The diff is 16 net lines, well inside the ≤ ~40 line budget.

## `used_memory` observations (3-point timeline, single shard, single DB)

| Checkpoint | `used_memory_human` | `used_memory_bytes` | % of 10 MB cap |
|------------|---------------------|---------------------|---------------:|
| Baseline (post compose-up, pre-ingest) | 4.70 M | 4 929 888 | 47.0 % |
| Post-ingest (2 500 rows, sleep 10) | **15.84 M** | **16 610 464** | **158.4 %** ❌ |
| Pre-teardown (post FT.INFO probe) | 15.88 M | 16 656 688 | 158.8 % |

**Delta attributable to this run's data + index + stream**: 11.14 MB for 2 500 rows ≈ 4.6 KB / row. At that observed rate, the maximum row count that fits inside the 10 MB absolute cap (after a 4.70 MB Redis-Cloud-standalone baseline) is **≈ 1 180 rows**, well below the 2 500-row target. The baseline alone (modules + persistence + replication overhead on this DB tier) already consumes 47 % of the absolute cap before any FRTB data lands.

## Peak vs 10 MB cap

**Peak `used_memory` = 15.88 MB = 158.8 % of the 10 MB cap → 💰 BUDGET-EXCEEDED.**

## Repo hygiene

- `git diff --stat services/api/src/ services/api/tests/`: 3 files (`src/index.ts`, `src/redis-ready.ts`, `tests/redis-ready.test.ts`), 36 insertions / 20 deletions = 16 net lines. Within the "≤ 3 files, ≤ ~40 lines net" budget.
- `git diff --name-only services/ | grep -v -E "services/api/src/redis-ready\.ts|services/api/src/index\.ts|services/api/tests/redis-ready\.test\.ts" | grep -E "services/.+/src/"` → **zero entries**. No drift outside the sanctioned 3 files.
- Secrets clean: the host-side check for credential-shaped tokens against this SUMMARY and the traceability pack returns **no matches** (CLEAN). No URL value, password, or auth material is echoed anywhere in `docs/recordings/smoke-run-15/` or `docs/demo/mar21-traceability.md`.

## Outcome classification

**💰 BUDGET-EXCEEDED.** The patch part of Wave 5.15r is GREEN and verified live (api reaches `bootstrap:ready` on a standalone Redis Cloud DB; FT.INFO confirms `num_docs=2500` with zero indexing failures), but the MVP-prove + traceability-update part is blocked by the 10 MB absolute `used_memory` cap, which is structurally infeasible at the configured 2 500-row dataset size given this DB tier's 4.70 MB resting overhead. Next wave needs an explicit knob change (raise cap, shrink dataset to ≈ 1 100 rows, or redefine the cap as a delta-over-baseline) before the live 6-variant matrix and the traceability Live column can be populated.
