{
  "name": "@frtb/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FRTB SBM PoV — Redis Stream consumer that writes JSON sensitivity docs via JSON.SET",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@frtb/redis-client": "^0.0.1",
    "@frtb/schema": "*",
    "ioredis": "^5.4.2",
    "pino": "^9.5.0",
    "tsx": "^4.19.2",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
